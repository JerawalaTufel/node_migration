{
  "name": "node_migration",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "migration:create" : "npx sequelize-cli migration:create",
    "migrate" : "npx sequelize-cli db:migrate",
    "migrate:rollback" : "npx sequelize-cli db:migrate:undo",
    "model:generate" : "npx sequelize-cli model:generate --name YourModelName"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.1",
    "express": "^4.18.2",
    "mysql2": "^3.9.1",
    "sequelize": "^6.36.0"
  },
  "devDependencies": {
    "sequelize-cli": "^6.6.2"
  }
}
